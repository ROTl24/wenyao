# Agent Note: 远程向量建库以完整批次断点和显式恢复为边界

Status: implemented

## Problem

远程向量建库在部分批次成功后可能收到服务商的 HTTP 400、402 或 429。旧流程只把 HTTP 状态归为通用失败，丢弃请求追踪与限额响应头；错误页仍提供“手动继续”，会在服务状态没有变化时再次发送同一失败批次。桌面端只保存成功的完整批次，PWA 则只在全部完成后保存向量，两端的向量文本模板也不一致。Windows 还可能在 `app-data.json` 短暂不可替换时返回 `EPERM`，使已经成功落盘的向量批次被报告为建库失败。

## Decision

桌面端与 PWA 共用服务商失败分类。公开错误只保留 HTTP 状态、允许的请求追踪 ID、Retry-After、ModelScope 限额响应头，以及经过脱敏的结构化错误码；PWA 不保存服务商响应消息。400 中存在明确限额证据时按限流处理，其他 400 与 422 按请求拒绝处理。

每个远程向量批次只有在响应数量和维度有效且本地断点落盘后才增加完成数。失败任务记录零基、右开区间的失败范围，并把向量能力测试状态设为失败。错误状态不允许直接调用继续接口；用户必须先重新执行一次向量最小测试，再从最后一个成功完整批次续建，或明确移除向量和重排能力，以生成模型加本地 BM25 完成配置。暂停状态仍可直接从断点继续。

PWA 在 Worker 内存中保留当前会话断点，并在 IndexedDB 中按批次保存向量块和连续完成元数据；最终索引成功保存后清理批次断点。向量文档统一由共享模板生成。PWA 旧文本格式与桌面格式不同，因此模板版本进入 PWA 缓存指纹；桌面模板保持原语义和原指纹，使已经付费完成的批次断点继续可用。

任何最小测试、远程建库批次和失败恢复都不自动重试。服务商状态未变化时，应用不会替用户重复发起可能计费的请求。

桌面状态文件使用进程级唯一临时文件完成原子替换。Windows 对目标文件的瞬时 `EPERM`、`EACCES`、`EBUSY` 或 `ENOTEMPTY` 拒绝只触发总计不超过 385 毫秒的本地文件替换重试；该重试发生在远程响应和向量断点已经成功保存之后，不会再次调用模型服务。持续失败时保留原状态文件并清理本次临时文件，向量构建仍从最后一个完整批次恢复。

## Alternatives considered

- 对网络错误、限流和服务端错误增加指数退避自动重试：无法确认失败请求是否已经计费，也会在账户限额或固定参数拒绝时重复发送同一批次。
- 仅修改错误文案，继续保留错误页的“手动继续”：无法从运行时阻止重复请求，其他调用入口仍可绕过界面保护。
- PWA 每批覆盖写入整个向量数组：实现简单，但会把逐批持久化放大为随进度增长的重复大对象写入。
- 状态文件替换失败后让整个建库直接终止：不会破坏已有向量断点，但把可恢复的瞬时 Windows 文件占用扩大为用户可见故障。

## Consequences

用户会看到失败批次范围、安全诊断、检查向量配置和跳过向量两条操作路径。服务恢复后需要一次明确的最小测试，但已成功的完整批次不会重复发送，升级前的桌面完整批次断点也保持可识别。Windows 的短暂文件占用不再中断进度提交。浏览器刷新后可从 IndexedDB 的连续批次断点恢复；IndexedDB 不可用时，当前页面仍可从 Worker 内存断点恢复，并明确提示刷新后可能需要从头构建。

## Verification

- `electron/services/provider-response-core.test.cjs` 覆盖 ModelScope 400 限额、未知 400 脱敏与 SiliconFlow 402 追踪信息。
- `electron/services/corpus-index.test.cjs` 和 `electron/services/ai-runtime.test.cjs` 覆盖失败范围、完整批次断点、错误续发阻断及重新测试前的状态。
- `src/lib/webAI/provider.test.ts`、`src/lib/webAI/worker.test.ts` 与 `src/components/AISetupWizard.test.tsx` 覆盖网页响应正文隔离、当前会话断点恢复和两条可见恢复路径。
- `npm run test:electron`、相关 Vitest、`npm run typecheck` 与 `npm run build:renderer` 完成离线验证。
- `electron/services/store.test.cjs` 注入两次 `EPERM`，验证状态文件原子替换在短暂占用后成功落盘且不遗留临时文件。
- SiliconFlow `Qwen/Qwen3-Embedding-4B` 完成 1263 条、1024 维真实建库。Windows 状态文件在 280 条处发生一次瞬时 `EPERM` 后，修复版本从该完整断点继续，最终索引包含全部 1263 个有序语料 ID；真实检索返回 40 个向量候选。整个验收新增 130 条向量调用账本记录且没有远程自动重试，密钥和账户响应正文未写入仓库或测试夹具。
